CREATE TABLE mpg (id real NOT NULL, QF real, IDF real, W string, PRIMARY KEY (id) );
CREATE TABLE cylinders (id real NOT NULL, QF real, IDF real, W string, PRIMARY KEY (id) );
CREATE TABLE displacement (id real NOT NULL, QF real, IDF real, W string, PRIMARY KEY (id) );
CREATE TABLE horsepower (id real NOT NULL, QF real, IDF real, W string, PRIMARY KEY (id) );
CREATE TABLE weight (id real NOT NULL, QF real, IDF real, W string, PRIMARY KEY (id) );
CREATE TABLE acceleration (id real NOT NULL, QF real, IDF real, W string, PRIMARY KEY (id) );
CREATE TABLE model_year (id real NOT NULL, QF real, IDF real, W string, PRIMARY KEY (id) );
CREATE TABLE origin (id real NOT NULL, QF real, IDF real, W string, PRIMARY KEY (id) );
CREATE TABLE brand (id string NOT NULL, QF real, IDF real, W string, PRIMARY KEY (id) );
CREATE TABLE model (id string NOT NULL, QF real, IDF real, W string, PRIMARY KEY (id) );
CREATE TABLE type (id string NOT NULL, QF real, IDF real, W string, PRIMARY KEY (id) );
CREATE TABLE bandwidth (id real NOT NULL, value real, PRIMARY KEY (id) );


